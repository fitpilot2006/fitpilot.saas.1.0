engine-strict=false
# This is a pnpm workspace monorepo.
# Always use pnpm, not npm or yarn.
# Running `npm install` will fail with "Unsupported URL Type workspace:"
# because workspace:* references are a pnpm-only protocol.
# Use: pnpm install
